Rule sets are loaded from %LOCALAPPDATA%\LokiClient\assets\rule-sets.
To override a built-in set, put russia-smart.zip, global.zip, whitelist.zip, or blacklist.zip in that folder.
Each zip must contain a JSON rule-set file with either { "rules": [...] } or a raw rules array.
